name: crdt
version: '0.1' # ComVer

dependencies:
  - base
  - vector

library:
  source-dirs: lib

tests:
  test:
    dependencies:
      - derive
      - QuickCheck
      - tasty
      - tasty-quickcheck

      - crdt

    main: Main.hs
    source-dirs: test
